#include "quiz.h"

template<class T>
Prob Quiz<T>::question(Dict<T>& dict, string uname = "admin")
{
    return Prob();
}

template<class T>
Prob QuizW2W::question(Dict<T>& dict, string uname = "admin")
{
    
}